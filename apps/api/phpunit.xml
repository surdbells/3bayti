<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/11.5/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
         failOnRisky="true"
         failOnWarning="true"
         cacheDirectory=".phpunit.cache"
         executionOrder="random"
         testdox="true">
    <testsuites>
        <testsuite name="3bayti API">
            <directory>tests</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>src</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="test" force="true"/>
        <!--
            Test signing secret — must be at least 32 bytes per
            JwtSettings::__construct's invariant. Static value is
            fine for tests; production reads JWT_SECRET from
            DigitalOcean App Platform's secret store.
        -->
        <env name="JWT_SECRET" value="test-secret-key-at-least-32-bytes-long-for-hs256-jwt-signing" force="true"/>
    </php>
</phpunit>
