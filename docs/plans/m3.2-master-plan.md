# M3.2 — Master Plan: Web Greenfield + M3.1.x Leftovers + Mobile Post-M3.1.7 Enhancements

**Status:** ⏸️ Awaiting approval gate on M3.2.0 detailed implementation plan
**Plan author:** Sodiq + Claude
**Approval gate status:** Plan approved (this commit). Per-phase plans need individual approval before execution begins.
**Entry point:** `dc2a9da` (M3.1.7-J closure)
**Companion docs:**
- `docs/plans/m3-plan.md` — original M3 master plan (still authoritative for M3.1.x done work, §1 migration discipline, §6 risk register)
- `docs/plans/m4-tier3-plan.md` — Tier 3 features deferred from M3 (separate scope)
- `docs/runbooks/m3/m3.1.7-completion.md` — M3.1.7 closure runbook (entry-point state)

---

## 0. Locked decisions

These were agreed at planning kickoff (May 16, 2026). Any change requires an explicit revision.

| # | Decision | Locked value |
|---|---|---|
| Q1 | M3.2 scope (X / Y / Z streams) | **Path A** — all three streams (X + Y + Z) under M3.2 |
| Q2 | Stream sequencing | **Strict sequential** (X → Y → Z) |
| Q3 | Quality gate infrastructure | **Playwright + Percy/Chromatic + automated a11y in CI** (built in M3.2.0 before any feature work) |
| Q4 | apps/web production status | **Not live**; mobile + portal are live |
| Q5 | Delivery commitment | **Whatever it takes, quality first** (~40-50 week realistic envelope split across M3.2 / M3.3 / M3.4) |
| Q6 | Feature richness | Multivendor ecommerce site with Tier 1 + Tier 2 features in M3; Tier 3 deferred to M4 |
| Q-Scope | Milestone split | **Split into M3.2 / M3.3 / M3.4** (cleaner closure docs, separate approval gates per milestone) |
| Q-QI | M3.2.0 = quality infrastructure first | **Yes** (1-week investment, ~40-50 week payoff across all surfaces) |
| Q-Web-Launch | apps/web public launch | **Stays private/staging until ALL of M3 complete** (web + mobile + portal all on v3) |
| Q-Features | Tier 1+2 in M3 / Tier 3 to M4 | **Path 1** — Tier 1+2 in M3 (separate M4 plan doc for Tier 3) |
| Q-Streams | Within M3.2, keep strict X→Y→Z | **Yes per Q2** (no parallelism within milestone) |
| Q-Snapshot | Per-commit status snapshot format | **Yes** — same format used throughout M3.1.7 (just-committed + plan position + up next) |

---

## 1. Milestone boundaries

```
M3.2 — Web greenfield UI + Stream X server leftovers + Stream Z mobile enhancements
   └ apps/web becomes feature-complete (cart/checkout/orders/account/wishlist/etc.)
   └ Stream X items unblock + clean up M3.1.x deferrals
   └ Stream Z items finish mobile post-M3.1.7 enhancements
   └ NEITHER apps/web NOR legacy retirement happen at the END of M3.2
   └ apps/web stays at staging.3bayti.ae throughout (Q-Web-Launch)

M3.3 — Portal migration to v3
   └ Build ~38 net-new admin endpoints
   └ Flip portal feature-by-feature with shadow mode
   └ Portal remains on legacy until each feature flipped
   └ apps/web STILL at staging.3bayti.ae

M3.4 — Cross-surface integration + legacy retirement
   └ RECONCILE-DELETES script + tested against snapshot
   └ EMAIL-CONFLICTS reset campaign (36 users)
   └ 7-day stability windows per surface (mobile / portal / web)
   └ apps/web goes live (Q-Web-Launch trigger)
   └ Legacy MariaDB read-only → archive → decommission
   └ M3 declared closed
```

Total envelope: **~40-50 weeks** of solo work from `dc2a9da` to M3 closure. Calendar-time depends on focus, interruptions, and how aggressively per-phase quality gates surface issues.

---

## 2. M3.2 phase ledger (~18-23 weeks)

### M3.2.0 — Quality infrastructure (~1 week) **← FIRST**

**Status:** ✅ Complete. See `docs/runbooks/m3.2/m3.2.0-completion.md` for closure runbook.

**Final commit:** TBD (M3.2.0-F closure)
**Phase span:** `8b85525` → closure

**Purpose:** Stand up Playwright + visual regression + automated a11y BEFORE any feature work, so every subsequent phase ships against these gates.

**Sub-phases:**

| # | Phase | Effort | Description |
|---|---|---|---|
| M3.2.0-A | Playwright setup for apps/web | 1-2 days | install, configure, first smoke test against home/catalog/categories, GitHub Actions workflow |
| M3.2.0-B | Playwright setup for apps/mobile (Capacitor + iOS sim + Android emulator) | 1-2 days | Capacitor-aware Playwright config, mobile smoke test, CI integration |
| M3.2.0-C | Visual regression with Chromatic OR Percy | 1-2 days | tool selection (Chromatic is Storybook-native + free for OSS-style projects; Percy is more flexible but paid); baseline snapshots for existing apps/web pages |
| M3.2.0-D | axe-core CI integration | 1 day | automated WCAG AA gate; first scan against existing pages; document any baseline violations to remediate inline as we ship new features |
| M3.2.0-E | Storybook for apps/web (component library scaffold) | 1-2 days | sets up component-driven development for the 11-15 week web build; foundation for Chromatic |
| M3.2.0-F | CI pipeline updates + closure docs | 0.5 days | GitHub Actions: gate every PR on phpunit + phpstan + tsc + Playwright + axe + Chromatic; update CONTRIBUTING.md; runbook for resetting baselines |

**Acceptance criteria:**

- [ ] Every existing apps/web page (home, catalog, categories) has a Playwright smoke test
- [ ] Chromatic/Percy baseline established for those pages
- [ ] axe-core passes on those pages OR violations documented for inline remediation
- [ ] CI rejects PRs with new Playwright/axe/Chromatic regressions
- [ ] Mobile Playwright runs locally (CI mobile testing deferred to per-need basis — emulators are expensive in CI)
- [ ] Storybook builds and serves components
- [ ] Runbook: how to reset visual baselines after intentional design changes
- [ ] Runbook: how to debug a Playwright failure locally

**No production touch:** all M3.2.0 work is tooling/CI. No runtime code changes to live surfaces.

---

### Stream X — M3.1.x leftovers (~3-4 weeks)

Server-side cleanup that unblocks subsequent work and closes M3.1 carry-forwards.

| # | Phase | Effort | Description |
|---|---|---|---|
| M3.2.X.1 | `best_sellers` + `best_sellers_listing` v3 endpoints + mobile flip | 2-3 days | ✅ **Code complete (c57f158)**. `GET /v3/products?sort=best_seller` shipped (-A 86454d3); mobile transforms + shadow wiring shipped (-C c57f158); closure runbook `docs/runbooks/m3.2/m3.2.x.1-completion.md`. Operator follow-up: 7-day shadow window then M3.2.X.1-C-FLIP. **Discovery: M3.2.X.1.5 follow-up needed** to migrate other catalog call sites before subsequent flag-aware Stream X phases (M3.2.X.2 etc.). |
| M3.2.X.1.5 | Mobile NetworkService → NetworkAdapter migration | 6-8 days | ✅ **Code complete (this commit)**. Migrated ~101 call sites across 33 files in 8 sub-phases A-H. Per Q-Scope=C, broader than catalog-only — includes payment, chat, mutations, vendor portal. Closure runbook `docs/runbooks/m3.2/m3.2.x.1.5-completion.md`. Debt registry `docs/runbooks/m3.2/mobile-adapter-migration-debt.md`. **Operator follow-up: staging Noon sandbox payment verification REQUIRED before production deploy** (sub-phase E flipped `POST /checkout/initiate` to v3). |
| M3.2.X.2 | `/v3/featured-vendors` endpoint + apps/web flip | 1-2 days | ✅ **Code complete (this commit)**. Shipped in 5 sub-phases (A-E). Backend `GET /v3/featured-vendors` with admin-managed `is_featured` curation + embedded top-4-newest products per vendor + SQL-aggregated rating. Apps/web feature flag flipped `target='old'` → `'new'`. Closure runbook `docs/runbooks/m3.2/m3.2.x.2-completion.md`. **Operator follow-up: run migration `Version20260516000001` on staging+prod, flag 3-4 vendors as featured via admin endpoint, verify Spotlight strip renders.** |
| M3.2.X.3 | `/v3/categories/:slug` augmentation with embedded products + meta | 1 day | ✅ **Code complete (this commit)**. Shipped in 4 sub-phases (A-D). Backend `GET /v3/categories/:slug` now returns `{data: CategoryDetail, meta: {total_products, page_size}}` with embedded 20-newest products, raw `product_count`, `image` (object form), `icon_name`, and `children` (Q-Children=A). Apps/web feature flag flipped `target='old'` → `'new'`. Closure runbook `docs/runbooks/m3.2/m3.2.x.3-completion.md`. **Operator follow-up: verify apps/web `/category/:slug` rendering on staging (Spotlight, breadcrumbs, JSON-LD, 404 path, empty-products variant).** |
| M3.2.X.4 | `notification_log` persistence table + wire to OrderNotificationService | 2-3 days | ✅ **Code complete (this commit)**. Shipped in 4 sub-phases (A-D). Backend: `notification_logs` table (migration `Version20260516000002`) with 6 indexes; `OrderNotificationService::safeSend` writes `sent`/`failed`/`skipped` rows on every code path with repository-failure isolation; `GET /v3/admin/notification-logs` with 8-filter set (order_id, template, status, recipient, error_kind, since, until, limit/offset), validation, `ACTION_VIEWED` audit emission. Closure runbook `docs/runbooks/m3.2/m3.2.x.4-completion.md`. **Operator follow-up: run migration on staging+prod, trigger a notification flow, verify rows appear, smoke-test admin endpoint with each filter, verify audit emission.** |
| M3.2.X.5 | Dispute eventType empirical capture + constant update | 1-2 days | ✅ **Code complete (this commit)**. Reframed May 18, 2026 — original "blocked on manual sandbox dispute trigger" approach replaced with observability hook + read-only audit script after the product decision to run against live Noon production credentials. `NoonWebhookController` now emits `noon.webhook.unknown_dispute_event_type` warning when an eventType contains `'dispute'` or `'chargeback'` substring but isn't in the recognized `DISPUTE_EVENT_TYPES` constant (auto-surfaces future unknown strings in production logs + Sentry). New `apps/api/bin/audit-dispute-event-types.php` operator-runnable script enumerates every distinct event_type ever seen in `payment_webhook_events`, flags unrecognized dispute-shaped strings, prints backfill SQL template. Mid-flight catch: PHP-DI nullable+default `LoggerInterface` was being short-circuited (same X.8-D pattern); made required, removed unused `NullLogger` import. +4 tests / +12 assertions. Closure runbook `docs/runbooks/m3.2/m3.2.x.5-completion.md`. **Operator follow-up: run audit script on staging + production; if unknown dispute eventTypes surface, add them to `DISPUTE_EVENT_TYPES` constant + backfill `order_disputes` rows using script's SQL template.** |
| M3.2.X.6 | Vendor lifecycle states (pending/approved/suspended) + `VendorAuthMiddleware` update | 4-5 days | ✅ **Code complete (this commit)**. Shipped in 5 sub-phases (A-E). Backend: `status` column on **Vendor entity** (NOTE: original plan text said "User entity" — corrected in -E based on pre-flight; the architecture is User has `is_vendor` boolean, Vendor has `owner_user_id` FK, so status logically belongs to the store). Migration `Version20260517000001` with backfill heuristic from legacy `is_store_approved` + `is_active` flags. State-transition methods on Vendor entity (approve / suspend / reactivate) with InvalidArgumentException on rejected transitions; `is_store_approved` kept in sync atomically. `VendorAuthMiddleware` now gates on `existsApprovedForOwnerUser` (lazy resolution per M3.2.X.4-B pattern); returns 403 `VENDOR_NOT_APPROVED` for users with no approved stores. 3 admin endpoints: `POST /v3/admin/vendors/{id}/approve|suspend|reactivate` with audit `ACTION_UPDATED` + status diff. 2 vendor self-serve endpoints in SEPARATE route group (Option I; AuthMiddleware only, bypasses lifecycle gate): `POST /v3/vendor/onboarding/submit` (creates pending vendor, flips is_vendor) + `GET /v3/vendor/onboarding/status` (lets pending/suspended vendors see their own state). Closure runbook `docs/runbooks/m3.2/m3.2.x.6-completion.md`. **Operator follow-up: run migration on staging+prod, review backfill results (especially soft-deleted-post-approved vendors that landed as suspended; pending vendors with live products that may need retroactive approval), smoke-test admin transitions + self-serve flow.** |
| M3.2.X.7 | Arabic email templates (i18n for 10 templates from M3.1.7-H) | 3-5 days | ✅ **Code complete (this commit)**. Shipped in 5 sub-phases (A-E). 9 user-facing templates translated to MSA (Modern Standard Arabic, formal register); admin dispute template locked to English per Q-VendorAdminLocale = A. New `LocaleResolver` service implements 4-branch decision tree (customer / vendor / admin / default). Customer locale routing UNIFIED on the existing `User.locale` field (M1.7.0; not a new field — the original plan's pre-flight missed this and added a redundant `User.preferred_locale` column; the Q-Unification pivot in sub-phase D reverted that and kept only the new `Vendor.preferred_locale` field, since Vendor genuinely had no existing locale column). Migration `Version20260518000001` adds vendor side only with CHECK constraint; no backfill (NULL = English fallback). `LocaleResolver::normalizeToShortTag()` strips region from BCP-47 (`'en-AE'` → `'en'`). `wrapHtml()` emits `<html lang="..." dir="ltr|rtl">` for proper RTL rendering in email clients. Existing English templates renamed `*En`; new Arabic methods `*Ar`. `render()` match expression delegates by `$isArabic = ($locale === 'ar')`. `OrderNotificationService` gains optional `?LocaleResolver` constructor injection (backwards-compat null default). `PUT /v3/admin/vendors/{id}` accepts `preferred_locale` field; `PATCH /v3/me/profile` already accepted `locale` field (M1.7.1 pre-existing). UAE business conventions preserved: order references stay Latin script, Western Arabic numerals (1,2,3 not ١,٢,٣), currency 'AED' or 'د.إ' via order.getCurrency(). +50 tests / +185 assertions across 5 sub-phases. Closure runbook `docs/runbooks/m3.2/m3.2.x.7-completion.md`. **Operator follow-up: run migration on staging+prod, smoke-test Arabic email send for customer + vendor, smoke-test admin dispute email STAYS English, native Arabic reviewer pass before production (recommended; not a hard blocker), communicate change to existing `users.locale='ar'` users who will start receiving Arabic emails upon deploy.** |
| M3.2.X.8 | **Tier 2** — Promo Code engine (catalog + resolver + customer endpoints + admin CRUD + order serializer) | 5-7 days | ✅ **Code complete (this commit)**. Shipped in 7 sub-phases (Plan, A-G). New `promo_codes` + `promo_redemptions` tables (migration `Version20260518000002`); 10-rule `PromoCodeResolverService` with structured `PROMO_*` error codes; `POST /v3/cart/quote` for live price preview; `POST /v3/checkout/initiate` now server-authoritative on promo (closes the legacy raw-discount security gap; emits `X-Bayti-Deprecation` header on legacy path); 5 admin CRUD endpoints under `/v3/admin/promo-codes` with full audit emission + soft-vs-hard delete branching (hard when zero redemptions); `applied_promo` block on `OrderSerializer::listShape`/`detailShape` reading from PromoRedemption snapshot columns so historical orders survive subsequent admin renames/re-prices unchanged. Mid-flight catch: PHP-DI nullable+default vs factory binding behavior (sub-phase D) — resolver made required. +107 tests / +386 assertions across 6 functional sub-phases. Closure runbook `docs/runbooks/m3.2/m3.2.x.8-completion.md`. **Operator follow-up: run migration on staging+prod, seed sample promo via admin endpoint, smoke-test admin CRUD round-trip + customer quote + checkout-with-promo + rejection paths + legacy deprecation header.** |
| M3.2.X.18 | **Tier 2** — Returns request flow (entities + photo storage + eligibility + 15 endpoints across customer/vendor/admin + notifications + OrderSerializer extension) | 7-10 days | ✅ **Code complete (this commit)**. Shipped in 9 sub-phases (Plan, A-I). New `order_return_requests` + `order_return_request_items` + `order_return_request_photos` + `order_return_refunds` tables (migration `Version20260518000003`); aggregate root with 7 statuses + 7 reason codes + state machine guards. Flysystem-backed `ReturnPhotoStorageService` for evidence photos (5 max, 5MB each, jpeg/png/webp, ULID-like 26-char hand-rolled path scheme, local in dev/staging, R2/S3-ready). `ReturnRequestEligibilityService` (14-day window, item-must-be-delivered, no-overlapping-pending) + `ReturnRefundCalculator` (pro-rated discount math via bcmath, half-up rounding). 15 HTTP endpoints: 5 customer (submit/list/detail/cancel/photo-serve with 3-branch auth for the latter), 3 vendor (list/detail/confirm-receipt — per Q-VendorRole vendor confirms physical receipt only, doesn't approve/deny), 7 admin (list/detail/approve/deny/mark-picked-up/record-refund). Manual refund recording off the Noon API per Q-Refund (bank_transfer/store_credit/cash/other with currency + reference + notes). 8 new email templates × locale variants = 14 customer/vendor render methods + 1 admin-only EN per Q-VendorAdminLocale; fan-out matrix matches Q-Notifications (submit→3 recipients, all other transitions→customer). OrderSerializer extended with optional `returns` summary in listShape/detailShape; customer + admin order-detail endpoints now embed return summaries inline (one extra query each, no N+1 on list). Mid-flight catches: pattern #19 locked (Doctrine-repo-consuming services bind via factory not autowire due to ClassMetadata::$name unguessable parameter); pattern #20 (notification dispatch is fire-and-forget with try/catch defense in depth in both service AND controller); pattern #21 (shaped phpdoc params with `= []` defaults need every key marked optional). +160 tests / +365 assertions across 8 functional sub-phases. Closure runbook `docs/runbooks/m3.2/m3.2.x.18-completion.md`. **Operator follow-up: run migration on staging+prod, configure photo storage env vars (`RETURN_PHOTO_STORAGE_PATH` for local; R2 vars for prod), smoke-test customer submit → admin approve → admin mark-picked-up → vendor confirm-receipt → admin record-refund full lifecycle, verify email fan-out + locale routing, schedule orphan-photo-cleanup cron (see operator-playbook §2.N).** |
| M3.2.X.10 | **Tier 2** — Faceted search backend (`FacetAggregator` + `ProductFilterParser` + `GET /v3/products/facets` + observability) | 5-7 days | ✅ **Code complete (this commit)**. Shipped in 6 sub-phases (Plan, A-E). New `FacetAggregator` service computing 5 disjunctive facets (size + color + price-bands + vendor + category) against the existing `products` table — no new tables, no new migrations (reuses GIN indexes on `available_sizes` / `available_colors` from `Version20260512000001`). Algolia-style disjunctive semantics: each facet drops its own filter when counting so the user can see what they'd switch to. Fixed 5-AED price bands per Q-Price-Banding = A (0-50 / 50-100 / 100-250 / 250-500 / 500+). Canonical size order XS/S/M/L/XL/XXL/XXXL with strnatcmp fallback for numeric shoe sizes. Per-facet cap at 50 values + 0-count suppression. New `GET /v3/products/facets` endpoint accepts the same query-string vocabulary as `GET /v3/products` plus `sizes[]` / `colors[]` refinement keys (array form OR comma-separated string). Public endpoint, no auth required. Soft-fail on unknown slugs (200 with empty data, not 404 — matches existing catalog semantics). New `ProductFilterParser` shared service extracted in -C centralizes slug-resolution + query-parsing for both ListProductsController (existing) and ListFacetsController (new); type-safe sentinels (int|null|false vs int -1 sentinel). Performance observability added in -D: PSR-3 timing logs (`facets.computed` at debug with duration + filter shape; `facets.slow_response` at warning when > 100ms — operator trigger to enable Redis caching per Q-Caching = A deferred). Defensive per-statement timeout (SET LOCAL statement_timeout = 2000ms; try/catch fail-soft for non-PostgreSQL test setups). New `InMemoryLogger` test helper in `tests/Support/` for asserting PSR-3 output. Mid-flight catches: dropped reflexive `final` keyword on `FacetAggregator` so PHPUnit can mock it from the controller test (no production impact, no subclasses exist); recurring PHP-DI nullable+default trap caught and fixed in -D before commit (logger made required, NullLogger import dropped). +62 tests / +201 assertions across 5 functional sub-phases. Closure runbook `docs/runbooks/m3.2/m3.2.x.10-completion.md`. **Operator follow-up: smoke-test the endpoint with each filter shape on staging, watch logs for `facets.slow_response` warnings during peak traffic — if sustained, schedule the Q-Caching = B Redis enablement task (~1 day work).** |
| M3.2.X.14 | **Tier 2** — Vendor performance metrics (`VendorMetricsCalculator` + 3 endpoints: admin single + admin list + vendor self-serve) | 3-5 days | ✅ **Code complete (this commit)**. Shipped in 6 sub-phases (Plan, A-F). New `VendorMetricsCalculator` service computing 4 status-derived rates per vendor over a rolling window: fulfillment (delivered/total items, Q-FulfillmentDef = A), cancellation (rejected/total items — vendor-initiated only, Q-CancellationDef = B), return (admin-approved returns/total items, Q-ReturnDef = B), dispute (disputed-orders/total-orders, Q-DisputeAttribution = A — multi-vendor disputes count once per vendor with items). No new tables — pure read-side aggregation over existing Order/OrderItem/OrderReturnRequest/OrderDispute. Window anchors on `orders.paid_at` (not created_at), defaults 30 days, clamped 7-365 (Q-Window = C). Per-window mode (Q-EmptyHandling = A): vendors with 0 items get null rates + 0 counts (distinguishes 'no data' from 'perfect performance'). Two public APIs: `computeForVendor(\$id, \$days)` runs 3 queries; `computeForVendorList(\$ids, \$days)` runs 3 queries TOTAL regardless of vendor count via GROUP BY vendor_id. Three new HTTP endpoints (Q-EndpointShape = A): `GET /v3/admin/vendors/{id}/metrics` (admin single-vendor, audited), `GET /v3/admin/vendor-metrics` (admin list across all vendors with status filter + dual-strategy sort: DB-pushdown for vendor fields, PHP-side for metric values with null-sorts-last semantics, audited with filter context), `GET /v3/vendor/metrics` (vendor self-serve, supports multi-store users via `?vendor_id=N`; missing vendor_id on multi-store returns 422 `VENDOR_AMBIGUOUS` with `available_vendor_ids` in details). PSR-3 observability matches the X.10-D pattern: `vendor_metrics.computed` debug + `vendor_metrics.slow_response` warning > 200ms (threshold tuned higher than X.10 since metric queries scan a 30-day order window vs indexed catalog rows). Defensive `SET LOCAL statement_timeout = 2000ms` with try/catch fail-soft. New `VendorMetricsSerializer` shapes single + list responses with canonical `{ data, meta }` envelope and per-vendor identity (id/slug/name) for client display. Integration test (X.14-F-style) verifies observability survives the full controller stack — catches future regressions where unit tests would still pass but production logs silently die. Mid-flight catches: HttpException ctor uses `status:` not `statusCode:`, `HttpException::forbidden()` is single-arg (no errorCode); test pattern initial draft used a non-existent `jsonAuthedRequest` helper — rewritten to use the established `JwtService::issueTokenPair` pattern. +53 tests / +198 assertions across 5 functional sub-phases. Closure runbook `docs/runbooks/m3.2/m3.2.x.14-completion.md`. **Operator follow-up: smoke-test all 3 endpoints on staging with a vendor that has 30+ days of orders, verify rates compute correctly + log `vendor_metrics.computed` debug entries land, watch for `vendor_metrics.slow_response` warnings — if sustained, schedule operator follow-up #18 (cache-warming when vendor count crosses ~500).** |
| M3.2.X.17 | **Tier 2** — Admin order timeline + vendor-scoped timeline (`OrderTimelineBuilder` + 2 endpoints: admin + vendor) | 3-5 days | ✅ **Code complete (this commit)**. Shipped in 7 sub-phases (Plan, A-F). New `OrderTimelineBuilder` domain service aggregating chronological order events from 5 distinct sources into a single feed: (1) entity-derived from Order (`order.created` from createdAt, `order.paid` from paid_at), (2) `audit_log` table (every recorded admin action against the order/items/returns via UNION ALL across the 3 subject types), (3) `notification_logs` (every email fired with template + sent_at + status), (4) `order_return_requests` (6 lifecycle timestamps per return: requested/decided/picked_up/delivered_to_vendor/refunded/cancelled), (5) `order_disputes` (created + resolved). 14 canonical event types per Q-EventTypes = A (order.*, notification.*, return.*, dispute.*). Canonical event shape per Q-EventShape = A (`{ id, type, occurred_at, actor, summary, details }` with namespaced source:id for collision-proof cross-source uniqueness). Configurable ordering (`?order=desc\|asc`, default newest-first per Q-Ordering = C) and pagination (limit + offset, default 50, max 200 per Q-Pagination = A). **Vendor scope built into the builder from day one** (Q-VendorScope = A): when `vendorIdFilter` is non-null, drops dispute events entirely; filters audit rows to OrderItem rows owned by the vendor (via item_vendor_id projected through UNION ALL); filters notifications to the vendor's contact_email; uses INNER JOIN on order_return_request_items + order_items in the returns query. Two new HTTP endpoints: `GET /v3/admin/orders/{id}/timeline` (admin scope, full visibility incl. disputes, audited with filter context) and `GET /v3/vendor/orders/{id}/timeline` (vendor scope with multi-store routing via `?vendor_id=N`, returns 422 VENDOR_AMBIGUOUS on missing param for multi-store users, opaque 404 for cross-tenant attempts, no audit emission since self-view). **X.17-B closed the vendor-transition audit gap**: TransitionVendorOrderItemController now emits `AuditEmitter::recordUpdate` on item-status changes with before/after snapshot (+ a second audit row when order-level status rolls to a new value). Without X.17-B the timeline would have had a hole when investigating "why was item X never shipped" — vendor-driven transitions were captured only in structured logs, never in audit_log. PSR-3 observability follows the X.14-A pattern: `order_timeline.computed` debug + `order_timeline.slow_response` warning > 300ms (threshold tuned higher than X.14's 200ms because timeline aggregates 5 sources vs metrics' 3). Defensive `SET LOCAL statement_timeout = 2000ms` with try/catch fail-soft. New `OrderTimelineSerializer` shapes the response envelope with order_id + order_reference in the meta block for client display without a second lookup. Integration test (X.17-E) verifies observability survives the full controller stack across both admin + vendor endpoints. Mid-flight catches: dropped `final` keyword from OrderTimelineBuilder so PHPUnit can mock it from controller tests (X.10-C precedent, no production impact); initial vendor-email splice in test fixture used wrong index (fetchNotificationEvents calls the notifications query BEFORE the vendor email lookup, not after); pagination test originally reused exhausted Connection mock across two build() calls — fixed by using two separate captureQueries handles. +48 tests / +151 assertions across 5 functional sub-phases. Closure runbook `docs/runbooks/m3.2/m3.2.x.17-completion.md`. **Operator follow-up: smoke-test both endpoints on staging with a fully-lifecycled order (paid + shipped + returned + disputed if possible), verify all 14 event types render correctly + log `order_timeline.computed` debug entries land, watch for `order_timeline.slow_response` warnings — if sustained on specific high-traffic orders, schedule operator follow-up #21 (timeline event archival).** |
| M3.2.X.11 | **Tier 2** — Abandoned cart recovery emails (`CartAbandonmentFinder` + `CartNotificationService` + `SendAbandonedCartRemindersCommand` cron + public unsubscribe endpoint) | 3-5 days | ✅ **Code complete (this commit)**. Shipped in 9 sub-phases (Plan, A-I). Two-column DB migration: `notification_logs.cart_id` (nullable FK ON DELETE SET NULL with partial index WHERE cart_id IS NOT NULL) + `users.marketing_emails_opt_out` (boolean default FALSE — opt-IN posture by default, required for UAE PDPL / Bahrain PDPL / Saudi PDPL compliance). New `EmailTemplate::CART_ABANDONED_CUSTOMER` case (Q-EventTypes locked) with `isMarketing()` helper distinguishing marketing-class templates from transactional ones (defense in depth at the enum level AND at the dispatch level). New `Cart::isAbandoned(DateTimeImmutable, DateInterval): bool` helper centralising the 3-condition eligibility policy. New `CartAbandonmentFinder` service (Q-EligibilityCriteria = A locked, all 5 criteria: status='active' AND user has email AND updated_at < cutoff AND EXISTS cart_items AND NOT EXISTS prior notification_log). NOT EXISTS guard enforces Q-MaxRemindersPerCart = A: exactly one reminder per cart, even if the customer later opts out (the SKIPPED log row written by the service acts as a 'persistent suppression marker' so the finder excludes the cart on all subsequent cron runs). Single SQL query returning cart_ids ordered by updated_at ASC (oldest-abandoned reminded first), LIMIT bounded by configurable batch size (default 100, max 500 per Q-BatchSize). PSR-3 observability follows the X.10-D pattern: `cart_abandonment_finder.computed` debug + `cart_abandonment_finder.slow_response` warning > 500ms (threshold tuned higher than X.10's 200ms because the query scans the full active-cart set with a NOT EXISTS subquery). New `CartEmailTemplateRenderer` (Q-RenderingApproach = B locked: parallel class to OrderEmailTemplateRenderer rather than refactoring its 1650+ lines to dual-purpose) with EN + AR templates for `cart.abandoned.customer` — Arabic uses singular 'منتج' / plural 'منتجات', RTL HTML wrap with proper `<html lang="ar" dir="rtl">`, defensive HTML escaping (XSS defense in user-supplied product names). Both renderers throw `\LogicException` on cross-routing (wiring-bug guard). New `UnsubscribeTokenIssuer` reusing JwtSettings::signingSecret with distinct `action: 'unsubscribe'` claim so access tokens can never accidentally satisfy unsubscribe and vice versa — verify() is opaque-on-failure for security (a recipient learning 'token expired' vs 'wrong action' could craft targeted retry patterns). 30-day TTL (Q-UnsubscribeFlow = A locked: long-lived because recipients may not open the email immediately). New `CartNotificationService::cartAbandoned(Cart $cart)` (Q-NotificationServiceShape = A locked: parallel service rather than adding cart methods to OrderNotificationService) with 3 eligibility checks each writing a STATUS_SKIPPED notification_log row with cart_id populated (the persistent-suppression-marker pattern). Opt-out gating is the only material difference from the order service: transactional emails IGNORE marketing_emails_opt_out (required for service function under PDPL); marketing emails HONOR it. Locale dispatch via User::getLocale() with BCP-47 normalization ('ar-AE' → 'ar') — LocaleResolver is order-tied and not reusable for the single-recipient cart context. Per-cart unsubscribe URL composed via `{APP_URL}/v3/notifications/unsubscribe?token=<signed-jwt>` and per-cart resume URL via `{APP_URL}/cart?cart_id=<id>` for the CTA button. New `SendAbandonedCartRemindersCommand` Symfony Console command (Q-CommandShape = A locked) with `--dry-run`, `--threshold-hours` (default 24, clamped [1, 168]), and `--batch-size` (default 100, clamped [1, 500]) options. Q-AbandonmentWindow = B locked: single threshold; multi-touch sequences (24h + 72h + 168h) deferred to operator follow-up #24 when conversion data justifies. Per-cart flush after each `cartAbandoned()` call preserves the persistent-suppression-marker guarantee even if a later cart crashes the process. Per-cart failure isolation via try/catch keeps the batch running; one cart's error doesn't abort the whole run (exit code FAILURE if any errors but batch still completes for ops visibility). Cart-disappearing-mid-run race handled quietly (logged at info, not counted as error). New `UnsubscribeController` at `GET /v3/notifications/unsubscribe?token=...` — public, no auth, returns HTML (not JSON) because it's hit from email clients. Q-UnsubscribeFlow = A locked: no-login UX required for PDPL Article 13 ('right to withdraw consent must be as simple as giving consent'). Decision tree: missing token → 400 generic page; invalid token (bad sig / expired / wrong action / malformed) → 400 SAME generic page (no info leak about WHY); user not found → 400 SAME page (no enumeration leak); already opted out → 200 success page (idempotent, no second flush); first unsubscribe → flag set + flush + 200 success page; persistence failure → 500 transient error page. Three branded HTML pages (success / error / transient-error) all using the same `wrap()` helper with inline styles only (email clients strip external CSS). Logger captures every call: `unsubscribe.invalid_token`, `unsubscribe.user_not_found`, `unsubscribe.already_opted_out`, `unsubscribe.persist_failed`, `unsubscribe.completed`. Q-AdminVisibility = A locked: the existing `/v3/admin/notification-logs` surface (X.4-C) already filters by template; adding `cart.abandoned.customer` to operator filter dashboards is sufficient for v1. Integration test (X.11-H) verifies the FULL cron stack — real Finder + real Service + real Renderer + real Token Issuer + InMemoryMailer + capturing logger — for both the happy path (eligible cart processed, 3 PSR-3 events fired through the stack, notification_log row written with cart_id) and the opt-out path (no email sent, SKIPPED log row written as persistent suppression marker). Mid-flight catches: initial draft used `\PDO::PARAM_*` constants for DBAL types (caught by phpstan — DBAL wants `ParameterType::*`); `NotificationLogRepository` is a class extending EntityRepository, not an interface (caught when designing the test capture helper); `MailerException` ctor signature is `(kind, message)` not `(message, kind)` (caught by reading source before running); `LocaleResolver` is order-tied and not reusable (caught when wiring CartNotificationService — replaced with local `normalizeLocale()` helper); adding the new enum case broke `OrderEmailTemplateRenderer::render`'s exhaustive match in both phpstan + 2 tests iterating `EmailTemplate::cases()` (fixed with defensive throw case in order renderer + cart-prefix skip in 2 all-cases test loops); `CartAbandonmentFinder` + `CartNotificationService` both shipped with `final` initially but the command test needs PHPUnit doubles (locked X.10-C / X.17-C pattern — dropped `final` + added `@internal` docblock); `UnsubscribeControllerTest::makeUser()` collided with the parent `HttpTestCase::makeUser()` (PHP fatal: access level conflict; renamed local helper); transient-error-page assertion `'try the link again'` failed because the page wraps text across newlines (fixed by asserting on `'Something went wrong'` which is on a single line). +68 tests / +210 assertions across 8 functional sub-phases. Closure runbook `docs/runbooks/m3.2/m3.2.x.11-completion.md`. **Operator follow-up: deploy two-column migration; smoke-test the cron command with `--dry-run` against a staging dataset that includes opted-out users + legacy carts without user_id; verify the unsubscribe endpoint returns the correct 400/200/500 statuses + HTML pages; verify the X.4-C admin notification-logs surface shows cart.abandoned.customer entries with cart_id populated; schedule cron at recommended every 1-2 hour cadence. Operator follow-ups #24-26 (multi-touch sequence, per-template opt-out, abandonment dashboard) added.** |
| M3.2.X.15 | **Tier 2** — Multi-currency display (`fx_rates` table + `CurrencyConversionService` + `CurrencyContextMiddleware` + `ProductSerializer` integration + admin endpoints) | 3-5 days | ✅ **Code complete (this commit)**. Shipped in 8 sub-phases (Plan, A-H). Single additive table `fx_rates` (id, base_code VARCHAR(3), target_code VARCHAR(3), rate NUMERIC(18,8), updated_at, updated_by_user_id with ON DELETE SET NULL) + UNIQUE(base_code, target_code) + INDEX on target_code. Seeded with 5 rows: AED→AED identity + AED→{USD, EUR, SAR, GBP} (Q-Currencies = A locked: UAE base + 4 highest-volume tourist origins). **Display-only multi-currency** — settlement remains 100% AED across Cart, Order, payment, payout, refund entities (Q-Scope = A locked). Customer-facing UX improvement so an international visitor browsing the catalog sees prices in their preferred currency alongside the canonical AED, can mentally evaluate offers without doing FX math, and is told at the cart/checkout step they'll be charged in AED. New `Currency` PHP backed enum with 5 cases + `fromQueryParamOrAed()` case-insensitive parser with Q-FallbackBehavior = B locked: unknown/empty/null returns AED rather than 422'ing browsing (stale clients passing removed currencies degrade gracefully). New `FxRate` Doctrine entity with constructor + setRate validation: regex `/^\\d+(\\.\\d+)?$/` rejects non-numeric/negative/exponential, bccomp >0 rejects zero (would zero out displayed prices since bcmath silently coerces bad input to 0), bccomp <1000 rejects base/target inversion typos (e.g. typing 367.0 instead of 0.272 for AED→USD). New `FxRateRepository::findAllRates()` ordered by target_code for the per-request load. New `CurrencyConversionService` (Q-RateShape = A locked: AED base, one multiplication per amount). Pure read-only service with `convert(\\$aedAmount, Currency \\$target): array{amount, currency, source_amount, source_currency, converted}` API. Decimal STRINGS throughout (no floats — bcmath handles multiplication; manual HALF_UP rounding via bias-add-then-truncate trick: bcadd value + 0.5\\*10^-DECIMALS at scale 8, then bcadd(., 0, DECIMALS) to truncate). Q-Rounding = A locked: 2 decimal HALF_UP (all v1 currencies use 2 fractional digits natively; per-currency rounding for future JPY/BHD/KWD/OMR deferred to operator follow-up #28). Identity short-circuit AED→AED returns the amount unchanged with `converted=false` WITHOUT any DB lookup or arithmetic (even with no FxRate repo wired). Missing-rate fallback: logs `fx_rate.missing` warning AND returns AED amount with `currency='AED'` + `converted=false` (browsing keeps working). Q-RateRefresh = C locked: sticky last-known rates with PSR-3 staleness warning. Rates older than `STALE_AFTER_HOURS = 48` emit `fx_rate.stale` warnings with `age_hours` + `threshold_hours` context per-call (per-call rather than per-load so admin dashboards see the warning every time a stale rate is hit). In-memory cache scoped to service instance — Slim DI is per-request so naturally resets between requests; no manual invalidation. New `CurrencyContextMiddleware` (Q-RequestContext = A locked: explicit `?currency=USD` query parameter only, no IP geolocation, no Accept-Language fallback). Public constant `ATTR_DISPLAY_CURRENCY = 'display_currency'`; sets attribute via `Currency::fromQueryParamOrAed`. Wired globally in `Bootstrap.php` between RoutingMiddleware and RequestIdMiddleware — pure middleware so global mount is safe (non-catalog routes simply don't read the attribute). `ProductSerializer` (X.15-E): optional constructor parameter `?CurrencyConversionService \\$conversion = null` (backward-compatible — existing call sites that instantiate via `new ProductSerializer()` keep working unchanged). Two new fluent methods: `withDisplayCurrency(Currency): self` and `configureFromRequest(ServerRequestInterface): self` (the latter reads the middleware attribute with defensive type check). `money()` helper rewritten to emit one of two shapes: AED single-amount (pre-X.15 shape preserved when service missing, AED configured, or missing-rate fallback) OR dual-amount with `source_amount` + `source_currency` keys when non-AED conversion succeeded — lets clients render 'GBP 78.29 (AED 365)' without a second roundtrip. Six catalog controllers updated with one-line chained `configureFromRequest(\\$request)` calls: ListProducts, GetProduct, GetProductByLegacyId, ListVendorProducts, ListVendorProductsByLegacyId, GetCategory. ListStylesController NOT modified — StyleSerializer emits raw decimal strings, not the {amount, currency} shape, and is out of v1 scope per Q-Scope = A. Admin endpoints (X.15-F): `GET /v3/admin/fx-rates` lists all 5 rates with per-row staleness flags (`is_stale` + `age_hours` computed against 48h threshold) and meta block carrying `supported_currencies` + `stale_after_hours` for the admin UI picker. `PUT /v3/admin/fx-rates/{target}` upserts the rate for AED→{target}: 3-letter regex constraint on path placeholder; defensive 422 if target=AED (would let an admin accidentally set 'AED→AED=0.5' and break the identity short-circuit); 404 if target not in supported list; 422 if body validation fails (delegated to FxRate::setRate). Audited via `AuditEmitter::recordUpdate` with before/after snapshot (Q-AdminVisibility = C locked: uses existing audit_log table with subject_type='FxRate' rather than a new audit infrastructure). New `snapshotFxRate` strategy added to AuditEmitter's match dispatcher capturing base_code, target_code, rate, updated_at, updated_by_user_id. Q-AdminUI = A locked: simple PUT upsert; bulk CSV deferred to operator follow-up #29. Integration test (X.15-G) verifies the FULL stack (middleware → controller → serializer → conversion service) wires up correctly with REAL service instances — bound CurrencyConversionService + ProductSerializer with constructor injection; mocked only FxRateRepository + ProductRepository. Covers happy path (fresh rate, no warnings), stale rate (>48h fires `fx_rate.stale` warning), missing rate (empty table fires `fx_rate.missing` warning + AED fallback), and AED short-circuit (no DB load, no warnings even when stale rates exist). Catches the failure mode where unit tests pass in isolation but a DI/wiring change silently breaks log propagation. Mid-flight catches: initial sed batch-edit accidentally touched ListStylesController which uses StyleSerializer (not ProductSerializer); phpstan caught `method.notFound` immediately and the edit was reverted (styles are out of v1 scope per Q-Scope = A); HttpTestCase::jsonRequest() requires array not null for the body parameter (test makeGet helper passed null → TypeError, fixed by passing empty array); AuditEmitter::recordCreate calls subjectId() which requires getId() to return int not null (mock EM's persist() doesn't trigger Doctrine's autoincrement; fixed by updating the persist mock callback to manually assign a unique id to newly-persisted FxRate entities); phpstan flagged `property.unusedType` on FxRate::\\$id (older entities have the same union but are in the implicit baseline); added `@phpstan-ignore-next-line` with explanatory comment. +51 tests / +200 assertions across 7 functional sub-phases. Closure runbook `docs/runbooks/m3.2/m3.2.x.15-completion.md`. **Operator follow-up: deploy migration; refresh seed rates with current real-world values via the admin endpoint before public launch; smoke-test the cron-like staleness alerting (manually backdate a rate's updated_at to 49+ hours past, hit the catalog endpoint with ?currency=USD, verify `fx_rate.stale` warning lands in logs); verify the X.4-C audit-log surface shows FxRate subject_type entries; schedule weekly operator task to refresh rates (until paid auto-refresh in #27). Operator follow-ups #27-31 added (paid auto-refresh, expand currencies, bulk CSV, request-level cache, staleness alerting).** |

**Acceptance gate before moving to Stream Y:**
- [ ] All Stream X sub-phases shipped and closed
- [ ] No new regressions in mobile or backend baselines
- [ ] Operator pre-flight for the deferred items completed (dispute eventType, vendor approval workflow tested with admin)

---

### Stream Y — Web greenfield UI (~11-15 weeks)

Greenfield Angular UI build on top of v3 endpoints that already exist. Each phase ships under the M3.2.0 quality gates (Playwright + Chromatic + axe).

#### M3.2.Y.1 — Web Auth UI Build + Flip (~5-7 days)

**Scope:** Login, register, OTP, reset password, JWT refresh.

**Sub-phases:**

| # | Phase | Description |
|---|---|---|
| M3.2.Y.1-A | Angular Auth service + state management (signals) | Token storage (HttpOnly cookie + memory fallback), refresh interceptor, route guards |
| M3.2.Y.1-B | Login page (`/login`) | Form + validation + error handling + i18n + a11y + Playwright test |
| M3.2.Y.1-C | Register page (`/register`) | Same patterns + password strength meter + email/phone validation |
| M3.2.Y.1-D | OTP verification + reset password flows | Multi-step, persistent across page reload |
| M3.2.Y.1-E | Header user menu + logout + JWT refresh | Auth-aware header; persistent session |
| M3.2.Y.1-F | Closure (Playwright + Chromatic + axe gates + runbook) | |

#### M3.2.Y.2 — Web Cart + Checkout + Orders Build (~6-8 weeks)

**Scope:** Cart icon in header, cart page, multi-step checkout, order history page, order detail page.

**Sub-phases (high level — to be detailed when M3.2.Y.1 closes):**

| # | Phase | Description |
|---|---|---|
| M3.2.Y.2-A | Cart state + header cart icon + cart drawer | Optimistic add-to-cart from product detail; live count badge |
| M3.2.Y.2-B | Cart page (`/cart`) | Full cart with quantity adjusters, remove, "save for later" stub, recommendations strip |
| M3.2.Y.2-C | Address book + new-address modal | Multi-address with default; integrates with checkout |
| M3.2.Y.2-D | Checkout flow part 1 — Shipping address selection | Step 1 of 3; preserves cart across navigation |
| M3.2.Y.2-E | Checkout flow part 2 — Order summary + promo code field | Step 2 of 3; shipping calculation (placeholder for M3.4 returns/shipping zones) |
| M3.2.Y.2-F | Checkout flow part 3 — Payment (handoff to Noon hosted page) | Step 3 of 3; integrates with M3.2.Y.3 |
| M3.2.Y.2-G | Order success page + email confirmation copy | Post-checkout return URL handler |
| M3.2.Y.2-H | Order history (`/account/orders`) | Paginated list with filter chips matching mobile UX |
| M3.2.Y.2-I | Order detail page (`/account/orders/:id`) | Full per-order detail including items, tracking, refund history, cancellation button |
| M3.2.Y.2-J | Customer cancel flow + refund display | Mirrors mobile cancel flow + refund request affordance |
| M3.2.Y.2-K | Closure (full e2e Playwright tests covering cart → checkout → order detail) | |

#### M3.2.Y.3 — Web Noon Payment Integration (~7-10 days)

**Scope:** Hosted-page redirect (NOT embedded SDK). User clicks "Pay" → redirected to Noon → returns to /checkout/return with order_reference.

**Sub-phases:**

| # | Phase | Description |
|---|---|---|
| M3.2.Y.3-A | `/checkout/return` route + status poll | Polls `GET /v3/checkout/status/:ref` every 2s up to 60s; shows success / pending / failed |
| M3.2.Y.3-B | Failure handling (gateway down, customer abandons) | Returns to checkout with cart intact; clear error messaging |
| M3.2.Y.3-C | Tests — Noon sandbox happy path + 3 failure scenarios | Real Noon sandbox calls in Playwright |

#### M3.2.Y.4 — Web Designer Routes (~3-5 days)

**Scope:** `/designer` index + `/designer/:slug` detail. Restore vendor entries to sitemap.xml.

**Sub-phases:**

| # | Phase | Description |
|---|---|---|
| M3.2.Y.4-A | Designer index page + grid layout | Uses existing `/v3/vendors` endpoint |
| M3.2.Y.4-B | Designer detail page | Vendor bio + products + reviews + follow button |
| M3.2.Y.4-C | Sitemap regeneration (restore 104 vendor URLs) | Updates `apps/web/scripts/generate-sitemap.mjs` |

#### M3.2.Y.5 — Web Account Management Build (~2-3 weeks)

**Scope:** Profile, addresses, wishlist UI, measurements, password change, account deletion request.

**Sub-phases:**

| # | Phase | Description |
|---|---|---|
| M3.2.Y.5-A | Account layout shell + sidebar nav | Shared layout for all account routes; auth-gated |
| M3.2.Y.5-B | Profile page — read + edit name/email/phone/avatar | Email/phone change confirmation flows |
| M3.2.Y.5-C | Address book CRUD UI | Add/edit/delete/set-default |
| M3.2.Y.5-D | Wishlist page | Grid layout, move-to-cart, remove, share |
| M3.2.Y.5-E | Measurements page | Form for body measurements (existing on mobile) |
| M3.2.Y.5-F | Password change flow | With OTP confirmation |
| M3.2.Y.5-G | Account deletion request | Soft-delete with 30-day reactivation window |

---

### Stream Z — Mobile post-M3.1.7 enhancements (~4 weeks)

Final mobile features deferred from M3.1.7-J known limitations.

| # | Phase | Effort | Description |
|---|---|---|---|
| M3.2.Z.1 | Customer per-order detail page (mobile) | 3-5 days | Full per-order detail screen on `/orders/:id` (currently inline on my-orders); shows items, tracking, refund history, cancel button (where applicable); matches Stream Y web order detail UX |
| M3.2.Z.2 | Mobile UX polish — multi-address book + saved address picker at checkout | 3-5 days | Mobile checkout currently uses single billing address; add address book parity with planned web UX |
| M3.2.Z.3 | Mobile UX polish — wishlist (basic) | 3-5 days | Wishlist add/remove from product detail; wishlist page accessible from account |
| M3.2.Z.4 | Push notifications — backend (FCM/APNs registration + send service) | 4-5 days | DeviceToken entity, registration endpoint, `PushNotificationService` mirroring `OrderNotificationService` patterns; FCM HTTP v1 + APNs HTTP/2 adapters |
| M3.2.Z.5 | Push notifications — mobile (Capacitor PushNotifications plugin + permission flow + handlers) | 3-5 days | Permission prompt at appropriate moment (not on first launch); deep link from notification to relevant screen |
| M3.2.Z.6 | Push notifications — wire to lifecycle events (mirror M3.1.7-H email triggers) | 2-3 days | Same 7 lifecycle moments fan-out to push as fan-out to email |

---

## 3. M3.2 Tier 2 features (interleaved per phase)

Per Q-Features, Tier 2 features ship in M3 if envelope permits. Most slot naturally into Stream Y phases; some need dedicated sub-phases. Mapping:

| Tier 2 feature | Where it ships |
|---|---|
| Promo codes | M3.2.Y.2-E (checkout summary) + dedicated backend phase **M3.2.X.8 (Promo Code engine)** ~5-7 days — ✅ **backend shipped (this commit)**; Y-side consumer surfaces still pending |
| ~~Saved payment methods~~ (Noon vault tokenization) | ❌ **Scoped out of M3 entirely (May 18, 2026 product decision)** — customers will not be allowed to save payment methods. Every checkout flow enters Noon's hosted page fresh. No M3.2.X.9 phase. Revisitable in M4+ if product priorities change. See `docs/runbooks/m3.2/stream-x-scope-revision.md` §Decision 2. |
| Faceted search (size/color/price/vendor/material) | **M3.2.X.10 (Faceted search backend)** ~5-7 days + M3.2.Y.2 catalog page update ~3-5 days |
| Abandoned cart recovery emails | **M3.2.X.11 (Cron + email template)** ~3-5 days |
| Recently viewed products | Lightweight client-side + backend persistence — slots into Stream Y as **M3.2.Y.6 (Recommendations strip)** ~3-5 days |
| Recommendations engine ("customers who bought X also bought Y") | Backend aggregation cron + admin UI; slots in as **M3.2.X.12** ~5-7 days |
| Vendor analytics dashboard | Backend aggregation + portal UI; technically belongs to M3.3 but we'll cover in **M3.2.X.13** to keep vendor experience momentum ~5-7 days |
| Vendor performance metrics (fulfillment rate, cancellation rate, dispute rate) | Backend computation + surfaced in admin orders surface (M3.1.7-D already has the audit data we need); slots in as **M3.2.X.14** ~3-5 days |
| Multi-currency display (AED + USD + SAR; settlement still AED) | **M3.2.X.15 (Currency display layer)** ~3-5 days |
| Apple Pay / Google Pay (via Noon express checkout) | Web phase **M3.2.Y.2-F** if Noon's docs support it via hosted page params; otherwise deferred |
| ~~Order tracking carrier integration~~ (Aramex/DHL/Naqel tracking numbers) | ❌ **Scoped out of M3 entirely (May 18, 2026 product decision)** — delivery is handled in-house by ops team manually creating delivery orders on each partner's portal. Customers receive email updates only; no in-app tracking numbers/URLs. No M3.2.X.16 phase. No Order entity columns for tracking. Revisitable if 3bayti later integrates carrier APIs. See `docs/runbooks/m3.2/stream-x-scope-revision.md` §Decision 3. |
| Order status timeline visualization for admin | **M3.2.X.17 (Admin timeline)** ~3-5 days |
| Returns request flow (Tier 1) | **M3.2.X.18 (Returns)** ~7-10 days — bumped to Tier 1 per ecommerce baseline |

**Tier 2 stream total: ~5-7 weeks** added to Stream X (revised May 18, 2026 — was 6-9 weeks; X.9 saved-payment-methods and X.16 order-tracking-carrier-integration scoped out per product decisions, reducing by ~8-12 days). New Stream X effort: **~8-11 weeks** (was 9-13).

Updated M3.2 envelope: **~22-30 weeks** (was 24-32 weeks; reduced by the Tier 2 scope-outs).

---

## 4. M3.3 phase ledger (~14-18 weeks; outlined, re-planned in earnest when M3.2 closes)

```
M3.3.0  Audit gap closure              ~1 week
        - Re-verify M3.1.0 endpoint inventory still accurate
        - Identify any drift since M3.1.0
        - Quality gate baselines refresh

M3.3.1  v3 Admin endpoints (Read)      ~3-4 weeks
        - ~20 admin GET endpoints: orders, users, commissions,
          processing, products, notifications, tickets,
          analytics, reports
        - Each with pagination, filtering, sorting per existing
          admin patterns from M3.1.7-D

M3.3.2  v3 Admin endpoints (Write)     ~3-4 weeks
        - ~18 admin POST/PATCH/DELETE endpoints
        - Each emits ACTION_OVERRIDDEN audit row per M3.1.7-D
        - Bulk action endpoints (Tier 2)

M3.3.3  PortalCrudAdapter layer        ~1 week
        - Mirror of MobileNetworkAdapter pattern
        - feature-flag-aware portal CRUD routing
        - Test infrastructure

M3.3.4  Portal flip (sequenced)        ~4-6 weeks
        - Per-feature: orders → users → commissions → products
        - Each with 7-day shadow + cutover per §1.4 plan doc
        - Strict migration discipline (portal is live)

M3.3.5  Portal remaining + edges       ~2-3 weeks
        - Long-tail admin features
        - Portal i18n if needed
        - Portal closure runbook
```

---

## 5. M3.4 phase ledger (~4-6 weeks; outlined)

```
M3.4.0  Pre-cutover audit              ~1 week
        - Verify all surfaces on v3
        - Verify all 7-day stability windows clean
        - Production health snapshot

M3.4.1  RECONCILE-DELETES script       ~1 week
        - Soft-deletes v3 rows missing from current legacy snapshot
        - Tested against snapshot before prod run

M3.4.2  EMAIL-CONFLICTS reset campaign ~1 week
        - 36 conflict-renamed users notified
        - One-click reset link flow
        - Manual support backup

M3.4.3  apps/web public launch         ~3-5 days
        - DNS cutover to live web
        - SEO submission to search engines
        - Sitemap regeneration final

M3.4.4  Legacy decommission             ~1-2 weeks
        - Legacy MariaDB → read-only
        - 7-day final stability window
        - Backup → archive → decommission
        - Cost cleanup (legacy infrastructure)

M3.4.5  M3 closure                      ~3 days
        - Final master runbook
        - Lessons-learned doc
        - M4 kickoff prep
```

---

## 6. Quality gate baselines (entry state, preserve through M3.2)

| Surface | Baseline | Tolerance |
|---|---|---|
| Backend phpunit | 535 tests, 1809 assertions | Must grow, never shrink |
| Backend phpstan | 60 errors in src/ | Must not grow |
| apps/api-client tsc | 0 errors | Must stay clean |
| apps/mobile tsc | 6 errors (legacy fixtures) | Must not grow |
| apps/web tsc | 0 errors | Must stay clean |
| Playwright apps/web | TBD baseline after M3.2.0 | Every new page adds tests |
| Chromatic baseline | TBD after M3.2.0 | Visual diffs reviewed per PR |
| axe-core | TBD after M3.2.0 | WCAG AA; no new violations |

---

## 7. Per-phase approval gate structure

**Per Q-Snapshot:** After every commit, Claude produces a status snapshot in this format:

```
## Status snapshot — M3.2.X.N (phase-id)

### Just committed
- Commit: <hash> <phase-id-and-letter> — <one-line summary>
- Files: N changed
- Tests Δ: +N tests / +N assertions
- Quality gates: phpunit ✓ phpstan ✓ tsc ✓ Playwright ✓ Chromatic ✓ axe ✓

### Plan position
| Milestone | Phase | Sub-phase | Status |
|---|---|---|---|
| M3.2 | X (leftovers) | M3.2.X.1 best_sellers | ✓ shipped |
| ... | ... | ... | ... |
| M3.2 | Y (web build) | M3.2.Y.1 auth UI | ▶ in progress |
| ... | ... | ... | ☐ pending |

### Up next
- <Next sub-phase> — needs detailed implementation plan + approval gate
- Awaiting approval on: <item>
```

Approval gates:

1. **Before any phase starts:** Claude presents a detailed sub-phase implementation plan; user explicitly approves before code writes
2. **After every commit:** Status snapshot per format above
3. **At end of each named phase (M3.2.0, M3.2.X.N, M3.2.Y.N, M3.2.Z.N):** Closure mini-runbook in `docs/runbooks/m3.2/` summarizing what shipped
4. **At end of M3.2 / M3.3 / M3.4:** Full closure runbook + master plan markers updated

---

## 8. Risks (M3.2-specific)

| Risk | Likelihood | Impact | Mitigation |
|---|---|---|---|
| Web greenfield UI takes longer than 6-8 weeks | High | Medium | Phase-by-phase approval gates; descope Tier 2 features to keep schedule |
| Q3 quality infrastructure delays first feature ship | Medium | Low | M3.2.0 is 1 week; if it stretches to 2 weeks, that's still <5% of envelope |
| Noon Apple Pay / Google Pay not available via hosted page | Medium | Low | Defer to M4; not blocking ecommerce-takes-orders bar |
| Faceted search performance on large catalog | Medium | Medium | Postgres GIN indexes + Elasticsearch fallback option if needed |
| Vendor performance metrics computation expensive | Low | Low | Cron-aggregated to materialized view; cached query |
| Push notification permission denials reduce reach | High | Low | Email channel (M3.1.7-H) is primary; push is enhancement |
| Mobile per-order detail page duplicates web detail page work | Low | Low | Share data shape + i18n strings; render differs by platform |
| Stream X items reveal more complexity than estimated | Medium | Medium | Per-sub-phase approval gate catches scope drift before code |

---

## 9. Non-goals for M3.2 (explicit deferrals)

These are NOT M3.2 even though they might come up:

- Portal migration (M3.3)
- Legacy retirement (M3.4)
- Tier 3 features (M4 — see `docs/plans/m4-tier3-plan.md`)
- Multi-warehouse vendor inventory
- BNPL (Tabby/Tamara)
- Gift cards
- Loyalty program
- Visual search
- A/B testing infrastructure
- Affiliate program
- Biometric login
- Offline mode (mobile)
- In-store pickup
- Customer segmentation
- Cohort analysis
- Webhook subscriptions for vendors
- API tokens for vendors

---

## 10. M3.2 approval gates

```
[ ] Locked decisions (§0) confirmed
[ ] Milestone split M3.2 / M3.3 / M3.4 (§1) approved
[ ] M3.2.0 quality infrastructure first (§2 M3.2.0) approved
[ ] Stream X scope + sub-phases (§2 Stream X + §3 Tier 2 integration) approved
[ ] Stream Y web build sub-phases (§2 Stream Y) approved
[ ] Stream Z mobile enhancements (§2 Stream Z) approved
[ ] Per-phase approval gate structure (§7) acknowledged
[ ] Risks (§8) acknowledged
[ ] Non-goals (§9) acknowledged
```

✅ This plan-document commit constitutes plan approval. Per-phase plans still need individual approval before execution begins (§7 item 1).

Next action: **draft M3.2.0 detailed implementation plan** for approval before any infrastructure code writes.
